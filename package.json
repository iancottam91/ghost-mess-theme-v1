{
  "name": "mess",
  "version": "0.0.2",
  "description": "Theme for food blog",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch:css": "onchange 'assets/**/*.scss' -i -- npm run build:css",
    "build:css": "node-sass ./assets/sass -o ./assets/css"
  },
  "author": {
    "email": "icottam@msn.com"
  },
  "license": "ISC",
  "dependencies": {
    "node-sass": "^4.5.1"
  },
  "config": {
    "posts_per_page": 10
  },
  "devDependencies": {
    "onchange": "^3.2.1"
  }
}
